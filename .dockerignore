# The corpus is indexed by a batch job against the database, not read by the service at
# runtime, so it has no business in the image.
corpus/
evals/
tests/
.git/
.github/
.venv/
venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
docker-compose.yml
Makefile
.mcp.json
