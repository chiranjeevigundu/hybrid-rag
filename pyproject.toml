[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ragkit"
version = "0.1.0"
description = "Hybrid retrieval (BM25 + dense) with reranking and a measured eval harness. No API keys."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    # Postgres driver. psycopg3 for native `executemany` batching and real
    # transaction context managers.
    "psycopg[binary]>=3.1",
]

[project.optional-dependencies]
# Split out so the core package stays importable — and unit-testable — without
# pulling ONNX Runtime. CI installs [dev] only and runs against HashingEmbedder.
embed = ["fastembed>=0.3"]
# The HTTP face. Not needed to use ragkit as a library or CLI.
serve = ["fastapi>=0.110", "uvicorn[standard]>=0.27"]
# The MCP face. Separate again, because a service-to-service caller has no use for
# tool schemas and an MCP client has no use for uvicorn.
mcp = ["mcp>=2.0"]
dev = ["pytest>=8", "ruff>=0.6", "httpx>=0.27", "fastapi>=0.110", "mcp>=2.0"]

[project.scripts]
ragkit = "ragkit.cli:main"
# Entry point for `claude mcp add hybrid-rag -- ragkit-mcp`
ragkit-mcp = "ragkit.mcp_server:main"

[tool.setuptools]
packages = ["ragkit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "postgres: needs a live Postgres with pgvector (skipped when DATABASE_URL is unset)",
    "model: downloads the real embedding model (~90 MB); off by default",
]
filterwarnings = ["error::DeprecationWarning:ragkit.*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "UP"]
